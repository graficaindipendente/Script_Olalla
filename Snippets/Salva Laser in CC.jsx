#target illustrator// Prompt per il nome del filevar docName = prompt("Inserisci il nome del file:", "nome-file");// Se il nome del file è vuoto o annullato, terminare lo scriptif (!docName) {    alert("Nome file non fornito. Script terminato.");    exit();}// Ottieni il percorso della cartella "Download"var downloadFolder;if ($.os.indexOf("Windows") !== -1) {    downloadFolder = Folder(Folder.userData.fsName + "/../Downloads");} else {    downloadFolder = Folder(Folder.userData.fsName + "/../Downloads");}// Crea il percorso completo del filevar filePath = downloadFolder.fsName + "/" + docName + ".ai";// Opzioni di esportazione per salvare come Illustrator CS3 (AI17)var saveOptions = new IllustratorSaveOptions();saveOptions.compatibility = Compatibility.ILLUSTRATOR17; // Salva come Illustrator 17 (CS3)saveOptions.pdfCompatible = false; // Evita di includere dati PDFsaveOptions.compressed = true; // File compressi per risparmiare spazio// Ottieni il documento attivovar doc = app.activeDocument;// Salva il documento con le opzioni specificatedoc.saveAs(new File(filePath), saveOptions);alert("File salvato in: " + filePath);